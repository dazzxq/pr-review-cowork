# ─── PR Review Agent — Cowork-Lite Config ──────────────────────────────────
# Copy file này thành `.env` rồi chỉnh giá trị nếu cần bật SEND_MODE.
#   cp .env.example .env
# `.env` đã được gitignore — KHÔNG commit secrets vào repo.

# ── Mode: gửi mail thẳng hay chỉ tạo draft? ─────────────────────────────────
# false  = chỉ tạo Gmail Draft (mặc định, an toàn — bạn approve thủ công)
# true   = gửi luôn qua SMTP (yêu cầu credentials bên dưới)
#
# Nếu set true mà credentials thiếu/sai → agent sẽ log error rồi FALLBACK
# về draft mode để không bỏ sót mail.
SEND_MODE=false

# ── SMTP credentials (CHỈ CẦN khi SEND_MODE=true) ──────────────────────────
# Email Gmail/Google Workspace dùng để gửi.
# Nên là email đã connect Gmail trong Cowork (để Sent Mail xuất hiện đúng nơi).
SENDER_EMAIL=

# App password 16 ký tự (chữ thường, có thể chứa dấu cách).
# Tạo tại: https://myaccount.google.com/apppasswords
# Yêu cầu: account đã bật 2-Step Verification.
# Nếu account là Workspace, admin có thể đã tắt App Password — liên hệ IT.
SENDER_APP_PASSWORD=

# ── SMTP server (mặc định OK cho Gmail/Workspace) ──────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
